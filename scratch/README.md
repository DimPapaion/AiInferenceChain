# scratch/

Temporary helper scripts used during development and testing.

**These files are NOT part of the InferenceChain protocol.**
They will be deleted before the project is released to main.

## Contents
- `train_cifar10_models.py` — trains 7 CNN models on CIFAR-10 and saves weights to `models/weights/`
