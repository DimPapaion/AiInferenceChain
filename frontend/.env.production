# When the React build is served by the FastAPI backend (e.g. via ngrok),
# leave these empty so API calls go to the same origin as the page.
# The client.js will auto-derive WS URLs from window.location.
REACT_APP_API_URL=
REACT_APP_WS_URL=
